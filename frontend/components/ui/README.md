/**
 * TraderOS UI foundation (Path A Phase 1).
 *
 * - Primitives live in this folder and are owned by TraderOS.
 * - Tokens come from `app/globals.css` + `lib/design-system/`.
 * - Import via `@/components/ui` or `@/components/ui/<name>`.
 *
 * Legacy APIs preserved:
 * - Button `kind="primary|ghost|danger"`
 * - Alert `kind="warn|danger|info"`
 * - Badge `status="ok|warn|…"`
 * - Panel, Stat, Field, KpiGrid, LimitBar, EmptyState
 */
export * from "./index";
