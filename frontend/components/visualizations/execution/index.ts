/**
 * Execution visualizations — bucket charts and scatters live in
 * `components/analytics/ExecutionLab.tsx` (ECharts). L1 answers:
 * `components/analytics/execution/ExecutionAnswerStrip.tsx`.
 * No visx dependency: DOM + ECharts cover all execution questions.
 */
export {};
