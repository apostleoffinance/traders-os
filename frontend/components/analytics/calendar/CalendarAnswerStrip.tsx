"use client";

/**
 * @deprecated Prefer PerformanceSnapshot via CalendarTab.
 * Kept for any external imports; forwards to the new snapshot when possible.
 */
export { CalendarAnswerStrip } from "@/components/trader/strips/CalendarAnswerStrip";
