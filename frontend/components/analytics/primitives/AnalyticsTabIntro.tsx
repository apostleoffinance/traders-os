"use client";

import type { AnalyticsPageId } from "@/lib/analytics/types";

/** No-op — page tabs already name the section. */
export function AnalyticsTabIntro(_props: { page: AnalyticsPageId }) {
  return null;
}
